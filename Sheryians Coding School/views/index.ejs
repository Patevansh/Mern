<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title></title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10">
            <form action="/create" method="POST">
                <input class="block w-full rounded-md outline-none text-white px-5 py-2 bg-zinc-800" type="text"
                    name="title" id="title" placeholder="title">
                <textarea class="block w-full rounded-md outline-none text-white mt-2 px-5 py-2 bg-zinc-800 resize-none"
                    name="details" id=""></textarea>
                <input class="mt-2 rounded-md bg-blue-600 px-5 py-2" type="submit" value="Create task">
        </div>
        <div class="tasks flex gap-3 flex-wrap p-10">
            <% if(files.length> 0){%>
                <% files.forEach(function(val){%>
                    <div class="task min-w-78 px-3 py-4 bg-zinc-800">
                        <h1 class="text-white text-3xl tracking-trighter">
                            <%= val%>
                        </h1>
                        <div>
                            <a class="text-blue-500 inline-block mt-3" href=<%=val%>>Read more.</a>
                            <a href="/edit/<%= val %>" class="text-zinc-600">edit</a>
                        </div>
                    </div>
                    <% }) %>
                        <% } else { %>
                            <h3 class="text-zinc-600">no task yet.</h3>
                            <% } %>
        </div>
    </div>
</body>

</html>